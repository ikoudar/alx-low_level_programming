#include "main.h"

/**
  *_isdigit- function that checks for digit.
  *
  *@c: c is a digit.
  *
  *Return: 1 if c is a digit, 0 otherwise.
*/

int _isdigit(int c)
{
	if (c != 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
